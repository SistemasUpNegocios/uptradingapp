<a href="" data-convenioid="{{ $convenioid }}" data-convenio="{{ $convenio }}" data-serie="{{ $serie }}" data-fecha_pago="{{ $fecha_pago }}" data-fecha_limite="{{ $fecha_limite }}" data-fecha_pagado="{{ $fecha_pagado }}" data-pago="{{ $pago }}" data-status="{{ $status }}" data-memo="{{ $memo }}" data-comprobante="{{ $comprobante }}" data-tipo_pago="{{ $tipo_pago }}" data-psnombre="{{ $psnombre }}" type="button" title="Vista previa" class="btn btn-sm btn-primary btn-sm btn-icon view"> <i class="bi bi-eye"></i></a>
@if (auth()->user()->is_root || auth()->user()->is_admin || auth()->user()->is_procesos || auth()->user()->is_egresos || auth()->user()->is_ps_diamond)
    <a href="" data-id="{{ $id }}" data-convenioid="{{ $convenioid }}" data-convenio="{{ $convenio }}" data-serie="{{ $serie }}" data-fecha_pago="{{ $fecha_pago }}" data-fecha_limite="{{ $fecha_limite }}" data-fecha_pagado="{{ $fecha_pagado }}" data-pago="{{ $pago }}" data-status="{{ $status }}" data-memo="{{ $memo }}" data-comprobante="{{ $comprobante }}" data-tipo_pago="{{ $tipo_pago }}" data-psnombre="{{ $psnombre }}" type="button" title="Editar pago ps" class="btn btn-sm btn-success btn-sm btn-icon edit"><i class="bi bi-pencil me-1"></i></a>
@endif