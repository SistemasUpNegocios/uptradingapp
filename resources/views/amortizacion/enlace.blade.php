@if (auth()->user()->is_root || auth()->user()->is_admin || auth()->user()->is_procesos || auth()->user()->is_ps_gold || auth()->user()->is_cliente || auth()->user()->is_cliente_ps_gold || auth()->user()->is_cliente_ps_silver || auth()->user()->is_egresos)
    <a href="" class="btn btn-sm principal-button new" id="seeAmortizaciones" data-contratoid="{{ $contratoid }}"><i class="bi bi-eye"></i>  Ver amortizaciones</a>
@endif
@if (auth()->user()->is_root || auth()->user()->is_admin || auth()->user()->is_procesos || auth()->user()->is_ps_gold || auth()->user()->is_egresos)
    <a href="" class="btn btn-sm principal-button new" id="seeContrato" data-nombrecliente="{{ $cliente_nombre }}" data-operador="{{ $operador }}" data-lugarfirma="{{ $lugar_firma }}" data-periodo="{{ $periodo }}" data-fecha="{{ $fecha }}" data-fecharen="{{ $fecha_renovacion }}" data-fechapag="{{ $fecha_pago }}" data-contrato="{{ $contrato }}" data-psid="{{ $psid }}" data-capertura="{{ $capertura }}" data-cmensual="{{ $cmensual }}" data-clienteid="{{ $clienteid }}" data-tipoid="{{ $tipoid }}" data-porcentaje="{{ $porcentaje }}" data-inversion="{{ $inversion }}" data-tipocambio="{{ $tipo_cambio }}" data-inversionus="{{ $inversion_us }}" data-inversionlet="{{ $inversion_letra }}" data-inversionletus="{{ $inversion_letra_us }}" data-status="{{ $status }}" data-fecharein="{{ $fecha_reintegro }}" data-statusrein="{{ $status_reintegro }}" data-memorein="{{ $memo_reintegro }}"><i class="bi bi-eye"></i> Ver contrato</a>
@endif
