<a href="" data-contratoid="{{ $contratoid }}" data-contrato="{{ $contrato }}" data-serie="{{ $serie }}" data-fecha_pago="{{ $fecha_pago }}" data-fecha_limite="{{ $fecha_limite }}" data-fecha_pagado="" data-pago="{{ $pago }}" data-status="{{ $status }}" data-memo="{{ $memo }}" data-comprobante="" data-tipo_pago="{{ $tipo_pago }}" data-psnombre="{{ $psnombre }}" type="button" title="Vista previa" class="btn btn-sm btn-primary btn-sm btn-icon view"> <i class="bi bi-eye me-1"></i>Ver pago a PS</a>

<a href="" data-id="{{ $id }}" data-contratoid="{{ $contratoid }}" data-contrato="{{ $contrato }}" data-serie="{{ $serie }}" data-fecha_pago="{{ $fecha_pago }}" data-fecha_limite="{{ $fecha_limite }}" data-fecha_pagado="" data-pago="{{ $pago }}" data-status="{{ $status }}" data-memo="{{ $memo }}" data-comprobante="" data-tipo_pago="{{ $tipo_pago }}" data-psnombre="{{ $psnombre }}" type="button" title="Editar pago ps" class="btn btn-sm btn-success btn-sm btn-icon edit"> <i class="bi bi-pencil"></i></a>