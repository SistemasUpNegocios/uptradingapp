<a href="" data-contratoid="{{ $contratoid }}" data-contrato="{{ $contrato }}" data-serie="{{ $serie }}" data-fecha_pago="{{ $fecha_pago }}" data-fecha_pagado="" data-pago="{{ $pago }}" data-status="{{ $status }}" data-memo="{{ $memo }}" data-comprobante="" data-tipo_pago="{{ $tipo_pago }}" data-clientenombre="{{ $clientenombre }}" type="button" title="Vista previa" class="btn btn-sm btn-primary btn-sm btn-icon view"><i class="bi bi-eye"></i></a>

@if (auth()->user()->is_root || auth()->user()->is_admin || auth()->user()->is_procesos || auth()->user()->is_egresos || auth()->user()->is_ps_diamond)
    <a href="" data-id="{{ $id }}" data-contratoid="{{ $contratoid }}" data-contrato="{{ $contrato }}" data-serie="{{ $serie }}" data-fecha_pago="{{ $fecha_pago }}" data-fecha_pagado="" data-pago="{{ $pago }}" data-status="{{ $status }}" data-memo="{{ $memo }}" data-comprobante="" data-tipo_pago="{{ $tipo_pago }}" data-clientenombre="{{ $clientenombre }}" type="button" title="Editar pago ps" class="btn btn-sm btn-success btn-sm btn-icon edit"> <i class="bi bi-pencil"></i></a>
@endif